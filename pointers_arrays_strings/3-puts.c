#include "main.h"
#include <stdio.h>
/**
 * _puts - prints a string followed by a new line
 * @str: pointer to the string to print
 *
 * Return: Nothing
 */
void _puts(char *str)
{
	int i = 0;

	while (i[str] != '\0')
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');

}
